
// Constant value of temperature in celsius
const celsius = 200;

// Constant value of temperature now converted to kelvin
const kelvin = celsius + 273;

// Value of temperature now converted to fahrenheit
let fahrenheit = celsius * (9 / 5) + 32;

// Round down fahrenheit variable
fahrenheit = Math.floor(fahrenheit);

// Output the data in string method
console.log (`The temperature is ${fahrenheit} degrees Fahrenheit.`);

// Value of temperature now converted to Newton
let newton = celsius * (33/100);

//Round down celsius to newton
newton = Math.floor(newton);

// Output the data in string method
console.log (`The temperature is ${newton} degrees Newton.`);

